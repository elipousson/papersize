% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_band.R
\name{plot_band_page}
\alias{plot_band_page}
\title{Arrange one or more paper bands on a page for printing}
\usage{
plot_band_page(
  paper,
  orientation = c("horizontal", "vertical"),
  n = NULL,
  pt = 10,
  x = NULL,
  thickness = NULL,
  overlap = NULL,
  band_width = NULL,
  fill = "white",
  color = "black",
  linewidth = 0.5,
  glue_fill = "grey85",
  fold_linetype = "dashed",
  fold_linewidth = linewidth/2,
  page = "letter",
  page_orientation = NULL,
  n_bands = 1,
  ncol = NULL,
  nrow = NULL,
  byrow = FALSE,
  gutter = NULL,
  margin = NULL,
  position = "center",
  unit = "in",
  marks = FALSE,
  paginate = TRUE
)
}
\arguments{
\item{paper}{Paper, page, or card name, or a data.frame with width and
height columns (as returned by \code{\link[=get_page_size]{get_page_size()}} or \code{\link[=make_page_size]{make_page_size()}}),
passed to \code{\link[=as_page]{as_page()}}. Sets the width and height of the stack of cards
or paper the band wraps around.}

\item{orientation}{Band orientation, either \code{"horizontal"} or
\code{"vertical"}. \code{"horizontal"} wraps the band left-to-right, crossing the
middle of the stack, so the band's panels use the \code{width} of \code{paper} and
the band's own height is set by \code{band_width}. \code{"vertical"} wraps the
band top-to-bottom instead, so the panels use \code{height} and the band's
own width is set by \code{band_width}. Default: \code{"horizontal"}}

\item{n, pt, x}{Passed to \code{\link[=as_thickness]{as_thickness()}} to calculate the thickness of the
stack. Ignored if \code{thickness} is supplied. \code{pt} is also used to add a
small ease allowance (the caliper of a single sheet or card) to the
folded side segments of the band, since a flat band can't wrap tightly
around a stack at its exact thickness. Ignored if \code{thickness} is
supplied.}

\item{thickness}{Optional. A \code{unit} object or number (in the units of
\code{paper}) with the thickness of the stack, overriding \code{n}, \code{pt}, and \code{x}.
If supplied, no ease allowance is added and \code{thickness} sets the folded
side segments of the band exactly. Default: \code{NULL}}

\item{overlap}{Distance the band extends past the far edge of the stack,
forming a segment that can be glued or taped closed. A \code{unit} object or
a number (in the units of \code{paper}). Default: \code{NULL}, which uses 40\% of
the \code{width} of \code{paper} (if \code{orientation} is \code{"horizontal"}) or 40\% of
the \code{height} of \code{paper} (if \code{orientation} is \code{"vertical"}) — i.e. 40\%
of whichever dimension of \code{paper} the band wraps around.}

\item{band_width}{Height of the band if \code{orientation} is \code{"horizontal"},
or width of the band if \code{orientation} is \code{"vertical"} — the dimension of
the band perpendicular to the direction it wraps around the stack. A
\code{unit} object or a number (in the units of \code{paper}). Default: \code{NULL},
which uses 40\% of the \code{height} of \code{paper} (if \code{orientation} is
\code{"horizontal"}) or 40\% of the \code{width} of \code{paper} (if \code{orientation} is
\code{"vertical"}).}

\item{fill, color, linewidth}{Fill, outline color, and outline linewidth for
the band, Default: \code{"white"}, \code{"black"}, and \code{0.5}}

\item{glue_fill}{Fill color used to distinguish the overlapping segment of
the band set by \code{overlap}, Default: \code{"grey85"}}

\item{fold_linetype, fold_linewidth}{Linetype and linewidth for the lines
marking where the band should be folded, Default: \code{"dashed"} and
\code{linewidth / 2}}

\item{page}{Paper name or a data.frame with width and height columns,
passed to \code{\link[=page_layout]{page_layout()}}. Default: \code{"letter"}}

\item{page_orientation}{Page orientation, passed to \code{\link[=page_layout]{page_layout()}}'s
\code{orientation} parameter (kept as a separate parameter here to avoid a
name clash with the band's own \code{orientation}). Default: \code{NULL}, which
uses \code{page} as supplied without reorienting it (unlike \code{\link[=page_layout]{page_layout()}}
itself, which defaults to \code{"landscape"}).}

\item{n_bands}{Number of copies of the band to arrange on \code{page}.
Default: \code{1}}

\item{ncol, nrow}{The dimensions of the grid to create.  If both are \code{NULL},
dims will be used or dims will be determined based on the plot dimensions.}

\item{byrow}{Analogous to \code{byrow} in \link[base:matrix]{matrix()}. If \code{FALSE} the
plots will be filled in in column-major order}

\item{gutter}{Optional. Spacing to add between plots in the grid, as a
single number (used for both row and column spacing), a length-2 numeric
vector \code{c(row, col)}, or a named vector or list with \code{row} and \code{col}
elements. Interpreted in \code{unit}. Implemented by adding half of \code{gutter}
to the interior-facing sides of each plot's own \code{plot.margin} (so two
adjacent plots each contribute half, summing to the full gutter between
them) based on its row/column position in the \code{ncol} x \code{nrow} grid —
this replaces each plot's existing \code{plot.margin}. Plots on the outer
edge of the grid are not padded on their outward-facing side; use
\code{margin} for space around the outside of the whole grid. Default: \code{NULL}
(no extra spacing).

\code{gutter} changes the total size of the combined grid (adding
\code{(ncol - 1) * col_gutter} and \code{(nrow - 1) * row_gutter}), which \code{marks}
accounts for automatically — the automatic \code{ncol}/\code{nrow} calculation
from \code{page}/\code{dims} also reserves room for it, so fewer plots may fit
per page than with \code{gutter = 0}.}

\item{margin}{Optional. A margin to add around the outside of the combined
grid of plots, e.g. so the grid can be centered on a larger sheet of
paper when saved with \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}. Passed to \code{\link[=get_margin]{get_margin()}} with
unit. The margin pads the composed page rather than the individual plots
in \code{plots}, and does not affect the number of rows and columns in the
grid. Default: \code{NULL}, which computes a margin from \code{position} whenever
plot dimensions are known (supplied via \code{dims}, or auto-detected from
the first plot) — see \code{position}. Set \code{margin} explicitly to override
that (or to add a margin when dimensions aren't known, e.g. \code{ncol}/
\code{nrow} supplied without \code{dims}).

\code{margin} itself always renders correctly regardless of \code{page}, because
it is applied as a fixed absolute-unit margin around whatever canvas
size \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} is eventually called with. \code{marks}, below, is
the one that depends on \code{page} being set correctly — see \code{marks}.}

\item{position}{Where to place the bands on \code{page} when \code{n_bands} doesn't
fill it completely. Passed to \code{\link[=page_layout]{page_layout()}}'s \code{position} parameter,
but defaults to \code{"center"} here rather than \code{\link[=page_layout]{page_layout()}}'s own
\code{"top-left"} default.}

\item{unit}{Unit used for \code{gutter}, and for \code{margin} if margin is a bare
numeric vector or list (ignored for \code{margin} if it is a \code{unit} class
object; \code{gutter} does not support \code{unit} class objects). Default:
\code{"in"}.}

\item{marks}{If \code{TRUE}, add crop marks in the \code{margin} area showing where
to cut the page into individual plots. Requires \code{margin}. Marks are
placed assuming the grid of plots exactly fills the page after
subtracting \code{margin}, i.e. the same assumption \code{margin} itself relies
on. Default: \code{FALSE}.

Unlike \code{margin}, \code{marks} reads \code{page} (via \code{\link[=get_page_dims]{get_page_dims()}}) to work
out where the margin area is, so \strong{\code{page} must equal the exact final
output size} — the same width/height you pass to \code{ggsave()} — even if
\code{ncol}/\code{nrow} are supplied directly and \code{page} would otherwise be
unused. Passing just the combined size of the grid of plots (i.e.
\code{page} without \code{margin} added on) will place the marks in the wrong
location, because \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} will render a larger canvas than
\code{marks} was calculated for.}

\item{paginate}{If \code{TRUE}, create a list of \code{patchwork} objects when the
number of plots is greater than the number of spaces in the plot layout.
Default to \code{TRUE}.}
}
\value{
A \code{patchwork} object, or (if \code{n_bands} doesn't fit on a single
\code{page} and \code{paginate = TRUE}) a list of \code{patchwork} objects — one per
page. Save with \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.
}
\description{
\code{\link[=plot_band_page]{plot_band_page()}} creates \code{n_bands} copies of a \code{\link[=plot_band]{plot_band()}} band and
arranges them, at their true printed size, in a grid on \code{page} — a thin
wrapper around \code{\link[=page_layout]{page_layout()}} (the same layout function used for a grid
of cards), which handles sizing the grid to the band's true dimensions,
shrinking it to fit if \code{n_bands} is fewer than \code{page} holds, positioning
that grid via \code{position}, gutter spacing, margins, crop marks, and
pagination when \code{n_bands} doesn't fit on one page. The result can be
saved directly with \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.
}
\examples{
\dontrun{
if (interactive() && is_installed(c("ggplot2", "patchwork"))) {
  plot_band_page(get_card("Poker"), n = 54, n_bands = 4)

  plot_band_page(
    get_card("Poker"),
    n = 54,
    n_bands = 1,
    position = "bottom-right"
  )
}
}
}
\seealso{
\code{\link[=plot_band]{plot_band()}}

\code{\link[=page_layout]{page_layout()}}
}
\keyword{card}
