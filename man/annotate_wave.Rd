% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_wave.R
\name{annotate_wave}
\alias{annotate_wave}
\title{Annotate a plot with a wavy (sine) line}
\usage{
annotate_wave(
  xmin,
  xmax,
  y = 0,
  amplitude = 1,
  period = 2,
  phase = 0,
  n = 200,
  color = "black",
  linewidth = 1,
  lineend = "round",
  linestyle = c("wave", "zigzag"),
  ...
)
}
\arguments{
\item{xmin, xmax}{Horizontal range the wave spans.}

\item{y}{Baseline (vertical center) the wave oscillates around, Default: 0}

\item{amplitude}{Height of the wave above and below \code{y}, Default: 1}

\item{period}{Horizontal distance the wave takes to complete one full
cycle, Default: 2}

\item{phase}{Horizontal shift of the wave, in radians, Default: 0}

\item{n}{Number of points used to draw the curve. Higher values produce a
smoother \code{"wave"} curve. Ignored for \code{"zigzag"}, Default: 200}

\item{color, linewidth, lineend}{Aesthetics passed to \code{\link[ggplot2:geom_line]{ggplot2::geom_line()}},
Default: \code{"black"}, \code{1}, and \code{"round"}}

\item{linestyle}{Either \code{"wave"} for a smooth sine curve or \code{"zigzag"} for
an angular zigzag (triangle wave) between the same peaks and troughs,
Default: \code{"wave"}}

\item{...}{Additional arguments passed to \code{\link[ggplot2:annotate]{ggplot2::annotate()}}.}
}
\value{
A ggplot2 annotation layer that can be added to a ggplot object.
}
\description{
\code{\link[=annotate_wave]{annotate_wave()}} draws a wavy line as a ggplot2 annotation layer, built
with \code{\link[ggplot2:annotate]{ggplot2::annotate()}} and \code{\link[ggplot2:geom_line]{ggplot2::geom_line()}}. Useful for
decorative dividers, underlines, or water/wave motifs.
}
\examples{
\dontrun{
if (interactive() && is_installed("ggplot2")) {
  ggplot2::ggplot() +
    annotate_wave(xmin = 0, xmax = 10, amplitude = 0.5, period = 2.2) +
    annotate_wave(
      xmin = 0,
      xmax = 10,
      y = -2,
      amplitude = 0.5,
      period = 2.2,
      linestyle = "wiggle"
    )
}
}
}
\keyword{annotate}
