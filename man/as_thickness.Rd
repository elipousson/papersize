% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_band.R
\name{as_thickness}
\alias{as_thickness}
\title{Calculate the thickness of a stack of cards or paper}
\usage{
as_thickness(..., n = NULL, pt = 10, x = NULL, units = NULL)
}
\arguments{
\item{...}{Not used.}

\item{n}{Number of sheets or cards in the stack. Ignored if \code{x} is
supplied.}

\item{pt}{Caliper thickness of a single sheet or card, in points (1/1000
in). Ignored if \code{x} is supplied. Default: 10, a typical thickness for a
playing card.}

\item{x}{Optional. A known stack thickness (numeric or \code{unit} object). If
supplied, \code{n} and \code{pt} are ignored.}

\item{units}{A character vector specifying the units for the
    corresponding numeric values.}
}
\value{
A \code{unit} object with the thickness of the stack.
}
\description{
\code{\link[=as_thickness]{as_thickness()}} estimates the thickness of a stack of \code{n} sheets of paper
or cards, each \code{pt} in caliper thickness (in points, i.e. thousandths of an
inch). Alternatively, supply a known thickness directly with \code{x}. Used by
\code{\link[=plot_band]{plot_band()}} to size the folded sides of a band that wraps around a
stack.
}
\examples{
as_thickness(n = 54)

as_thickness(x = 0.75, units = "in")
}
